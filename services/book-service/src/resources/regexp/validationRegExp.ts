/**
 * Regular expressions used in validation processes.
 * @module src/resources/regExp/validationRegExp
 */

/**
 * Regular expression used in checking only-letter string validity.
 * @type {RegExp}
 */
export const ALPHABETIC_REGEX = new RegExp(/^[A-Za-z]+$/);
