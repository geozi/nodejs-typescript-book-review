/**
 * Common constants.
 * @module src/resources/constants/common.constant
 */

/**
 * Contains numeric constant(s) used in express-validator operations.
 *
 * @type {object}
 * @property {number} MONGODB_ID_LENGTH - The length of a MongoDB ObjectId.
 */
export const commonConstants = {
  /**
   * The length of a MongoDB ObjectId.
   * @type {number}
   */
  MONGODB_ID_LENGTH: 24,
};
